<p>
  <strong>
    Title:
  </strong>
    <%= @article.title %>
</p>

<p>
  <strong>
    Text:
  </strong>
    <%= @article.text %>
</p>

<h2>
  Comments
</h2>

<%= render @article.comments %>
<!--this will render to _comments.html.erb with each comment that is in @article.comments -->
<!--collection.it assigns each comment to a local variable name the same as the partial-->
<!--in this case comment which is then available in the partial for us to show-->


<h2>Add Comment</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit' ,edit_article_path(@article) %> |
<%= link_to 'Back' , articles_path %>