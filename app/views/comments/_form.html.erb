<%= form_with model: [ @article,@article.comments.build ] do |form| %>
  <p>
    <%= form.label :commenter %><hr>
    <%= form.text_field :commenter %>
  </p>

  <p>
    <%= form.label :body %><hr>
    <%= form.text_area :body %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>